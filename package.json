{
  "dependencies": {
    "@emailjs/browser": "^3.2.0",
    "@google-cloud/local-auth": "^3.0.1",
    "google-auth-library": "^9.10.0",
    "googleapis": "^140.0.0",
    "three": "^0.168.0"
  },
  "devDependencies": {
    "vite": "^4.5.5"
  },
  "scripts": {
    "d": "sed -i '' -e 's/type=\"module\" src/src/' index.html && vite --host",
    "b": "sed -i '' -e 's/t src/t type=\"module\" src/' index.html && vite build",
    "p": "vite preview --host",
    "build": "vite build"
  }
}
